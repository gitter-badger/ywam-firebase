import template from './baseSetup.html';
import controller from './baseSetup.controller';
import './baseSetup.styl';

let baseSetupComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default baseSetupComponent;
