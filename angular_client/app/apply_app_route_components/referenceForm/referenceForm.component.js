import template from './referenceForm.html';
import controller from './referenceForm.controller';
import './referenceForm.styl';

let referenceFormComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default referenceFormComponent;
