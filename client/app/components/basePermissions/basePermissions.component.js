import template from './basePermissions.html';
import controller from './basePermissions.controller';
import './basePermissions.styl';

let basePermissionsComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default basePermissionsComponent;
