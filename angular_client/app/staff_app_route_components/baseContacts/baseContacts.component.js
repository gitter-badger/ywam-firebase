import template from './baseContacts.html';
import controller from './baseContacts.controller';
import './baseContacts.styl';

let baseContactsComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default baseContactsComponent;
