import template from './staff.html';
import controller from './staff.controller';
import './staff.styl';

let staffComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default staffComponent;
