import template from './baseNotices.html';
import controller from './baseNotices.controller';
import './baseNotices.styl';

let baseNoticesComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default baseNoticesComponent;
