import template from './apply.html';
import controller from './apply.controller';
import './apply.styl';

let applyComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default applyComponent;
