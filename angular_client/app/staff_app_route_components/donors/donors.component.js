import template from './donors.html';
import controller from './donors.controller';
import './donors.styl';

let donorsComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default donorsComponent;
